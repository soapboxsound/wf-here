CONTEXT LINE — paste this first, then paste the brief below it.

We are building WF-Here, a map-based workspace finder. The design system is set up in /css/tokens.css. Do not change any existing files unless I explicitly ask. Always reference tokens.css values rather than hardcoding colors, fonts, or spacing.

----------------------------------------------------------------

BRIEF 8 — PLACE DETAIL PAGE

Build the place detail page at /place/index.html. The nav component is already in the file. Modify the nav and add all content below it. All CSS goes in /css/components.css plus a style tag in the page head for page-specific layout.

MODIFIED NAV FOR DETAIL PAGE

Replace the logo with a back arrow and city name on the left: an anchor tag linking to /explore/index.html containing an icon with class "ti ti-arrow-left" at font-size 13px, followed by the text "New York". Font-size var(--text-sm), color var(--color-ink-muted), display flex, align-items center, gap 4px, text-decoration none.

Add a "share" text link on the right: font-size var(--text-sm), color var(--color-ink-muted), cursor pointer.

PHOTO HERO

A div with class "place-hero". Display grid, grid-template-columns 1fr 1fr, height 260px. No gap.

Left cell: main photo placeholder. Background #CCCAC4, display flex, align-items center, justify-content center. An icon with class "ti ti-photo" at font-size 32px color #B8B4AC.

Right cell: a 2x2 grid of thumbnail placeholders. Display grid, grid-template-columns 1fr 1fr, grid-template-rows 1fr 1fr, gap 2px. Each thumbnail: background varying slightly — first #C8C4BC, second #D0CCC4, third #DDDDD8, fourth #C4C0B8. Position relative on the right cell. Add a photo count badge: position absolute, bottom 10px, right 10px, background rgba 0 0 0 0.55, color white, font-size var(--text-xs), padding 3px 8px, border-radius var(--radius-md), content "8 photos".

MAIN CONTENT AREA

A div with class "place-content". Max-width var(--content-max-width) which is 860px, margin 0 auto, padding 2rem, display grid, grid-template-columns 1fr 300px, gap 2.5rem, align-items start.

MAIN COLUMN

Spot header div with class "spot-header". Display flex, flex-direction column, gap 8px.

Type and neighborhood line: a p tag containing "café" then a dot separator then "Lower East Side, New York". Font-size var(--text-sm), color var(--color-ink-muted).

Heading: an h1 containing "Ludlow Coffee Supply". Font-size 32px, font-weight var(--weight-light), color var(--color-ink), letter-spacing -1px, line-height 1.1.

Meta row: a div with display flex, align-items center, gap 16px, flex-wrap wrap. Containing: wifi quality indicator div with a green dot (6px, border-radius 50%, background #27A56B) and text "great wifi" (font-size 13px, color #27A56B, font-weight var(--weight-medium)), a dot separator, "open now" in color #27A56B, a dot separator, "closes 6pm" in color var(--color-ink-muted), a dot separator, "free to sit" in color var(--color-ink-muted).

Details section. A section-title p tag: "The details" — font-size var(--text-sm), font-weight var(--weight-medium), color var(--color-ink), margin-bottom 10px.

Attributes grid: display grid, grid-template-columns repeat auto-fit minmax 140px 1fr, gap 8px.

Eight attribute cards. Each card: class "attr-card", background var(--color-surface-raised), border var(--border-hairline), border-radius var(--radius-lg), padding 10px 12px. Inside each: a label p tag (font-size var(--text-xs), text-transform uppercase, letter-spacing 0.06em, color var(--color-ink-muted)) and a value p tag (font-size var(--text-sm), font-weight var(--weight-medium), color var(--color-ink)).

The eight cards with their labels and values:

Label "Wifi speed", value "Fast · 85 Mbps" in color #27A56B.
Label "Noise level", value "Quiet to moderate" in var(--color-ink).
Label "Power outlets", value "Plenty" in color #27A56B.
Label "Food and drinks", value "Coffee + food" in color #27A56B.
Label "Seating", value "Tables + bar" in var(--color-ink).
Label "Cost to sit", value "Free · buy something" in var(--color-ink).
Label "Hours", value "7am – 6pm daily" in var(--color-ink).
Label "Bathrooms", value "Yes · clean" in color #27A56B.

Wifi section. A section-title p tag: "Wifi".

A div with class "wifi-card". Background var(--color-surface-raised), border var(--border-hairline), border-radius 10px, overflow hidden.

Header row: padding 10px 14px, display flex, align-items center, justify-content space-between, border-bottom var(--border-hairline). Left: a green dot (6px) and the text "LudlowCoffee_Guest" in font-weight medium font-size var(--text-sm). Right: "85 Mbps · great" in font-size var(--text-xs) color #27A56B.

Password row: padding 10px 14px, display flex, align-items center, gap 12px. Left: label "Password" in font-size var(--text-xs) color var(--color-ink-muted), min-width 58px. Center: locked state div with class "wifi-pw-locked" — display flex, align-items center, gap 6px, background var(--color-surface), border var(--border-hairline), border-radius var(--radius-md), padding 5px 10px, flex 1, font-size var(--text-sm), color var(--color-ink-faint). Inside: an icon with class "ti ti-lock" at font-size 13px color var(--color-ink-faint), and the text "sign in to reveal".

Login nudge bar: padding 8px 14px, background var(--color-surface), border-top var(--border-hairline), display flex, align-items center, justify-content space-between. Left: "Create a free account to see wifi passwords" — font-size var(--text-sm), color var(--color-ink-muted). Right: "sign in ↗" — font-size var(--text-sm), color var(--color-ink), font-weight var(--weight-medium), background transparent, border var(--border-hairline), border-radius var(--radius-md), padding 4px 12px, cursor pointer.

Bathroom section. A section-title p tag: "Bathroom".

A div with class "bathroom-card". Background var(--color-surface-raised), border var(--border-hairline), border-radius 10px, overflow hidden.

Header row: padding 10px 14px, display flex, align-items center, justify-content space-between.

Left side: display flex, align-items center, gap 8px. An icon with class "ti ti-door" at font-size 16px color var(--color-ink-muted). A div containing two lines — "Single · gender neutral" (font-size var(--text-sm), font-weight var(--weight-medium), color var(--color-ink)) and "keypad code required" (font-size var(--text-xs), color var(--color-ink-muted)).

Right side: a button with id "revealBtn". Background transparent, border var(--border-hairline), border-radius var(--radius-md), padding 5px 12px, font-size var(--text-sm), color var(--color-ink), cursor pointer, font-family inherit, display flex, align-items center, gap 5px. An icon with class "ti ti-eye" plus the text "reveal code".

Revealed content: a div with id "bathroomRevealed" — hidden by default using display none. Padding 0 14px 14px. Contains: a div with class "code-display" showing a div with class "code-value" containing "4281" (font-family var(--font-mono), font-size 22px, font-weight var(--weight-medium), color var(--color-ink), background var(--color-surface), border var(--border-hairline), border-radius var(--radius-lg), padding 8px 16px, letter-spacing 4px) and a copy button next to it (font-size var(--text-xs), background transparent, border var(--border-hairline), border-radius var(--radius-md), padding 5px 10px, cursor pointer). Below the code display: a disclaimer p tag "codes change — verify with staff if this doesn't work" — font-size var(--text-xs), color var(--color-ink-faint), display flex, align-items center, gap 4px, margin-top 6px. Add icon "ti ti-info-circle" before the text.

JavaScript for reveal button: clicking the reveal button toggles display of bathroomRevealed between none and flex, and toggles the button text between "reveal code" and "hide". Change the eye icon to "ti ti-eye-off" when revealed.

JavaScript for copy button: clicking copies "4281" to clipboard and temporarily changes button text to "copied" for 1500ms.

Vibe section. A section-title p tag: "The vibe".

A div with class "vibe-tags" — display flex, flex-wrap wrap, gap 6px. Containing pill tags using the chip CSS. Tags: "neighborhood gem", "laptop friendly", "good music", "not too crowded", "natural light", "regulars crowd".

Reviews section. A div with class "reviews-section". Header: display flex, align-items center, justify-content space-between, margin-bottom 10px. Left: section-title "Reviews". Right: "12 reviews" — font-size var(--text-sm), color var(--color-ink-muted).

Two review cards, each with class "review-card": background var(--color-surface-raised), border var(--border-hairline), border-radius 10px, padding 14px, display flex, flex-direction column, gap 8px.

Review card header: display flex, align-items center, justify-content space-between. Left: reviewer avatar (width 28px, height 28px, border-radius 50%, background var(--color-surface-sunken), display flex, align-items center, justify-content center, font-size var(--text-xs), font-weight var(--weight-medium), color var(--color-ink-muted)) and reviewer name (font-size var(--text-sm), font-weight var(--weight-medium), color var(--color-ink)). Right: date (font-size var(--text-xs), color var(--color-ink-faint)).

Review 1: avatar initials "JM", name "Jamie M.", date "2 weeks ago", text "My go-to in the LES. Wifi never drops, there's always a seat at the bar if the tables are full, and nobody gives you a look for camping out for three hours.", tags "great wifi" and "good seating" and "laptop friendly".

Review 2: avatar initials "SC", name "Sara C.", date "1 month ago", text "Gets a little noisy around noon but clears up by 2pm. Coffee is genuinely good, which makes the whole thing better.", tags "good coffee" and "gets busy midday".

Review tag styles: font-size var(--text-xs), padding 2px 8px, background var(--color-surface), border var(--border-hairline), border-radius var(--radius-full), color var(--color-ink-muted).

Write review placeholder below cards: display flex, align-items center, gap 8px, background var(--color-surface-raised), border var(--border-hairline), border-radius 10px, padding 12px 14px, cursor pointer. An icon with class "ti ti-pencil" at font-size 14px color var(--color-ink-faint), and a p tag "Write a review — sign in first" in font-size var(--text-sm), color var(--color-ink-faint).

Removal request link at bottom of main column: a p tag centered, font-size var(--text-xs), color var(--color-ink-faint), margin-top 1rem. Text: "Is this your business?" followed by an anchor tag "Request removal ↗" with color var(--color-ink-muted), underline.

SIDEBAR COLUMN

Position sticky, top calc var(--nav-height) plus 20px. Display flex, flex-direction column, gap 12px.

Action card: class "action-card", background var(--color-surface-raised), border var(--border-hairline), border-radius var(--radius-xl), padding 1.25rem, display flex, flex-direction column, gap 12px.

Save button: full width, padding 11px, background var(--color-ink), color var(--color-surface), border none, border-radius var(--radius-lg), font-size var(--text-md), cursor pointer, font-family inherit, display flex, align-items center, justify-content center, gap 8px. Contains icon "ti ti-heart" and text "save this spot".

Directions button: full width, padding 11px, background transparent, color var(--color-ink), border var(--border-hairline), border-radius var(--radius-lg), font-size var(--text-md), cursor pointer, font-family inherit, display flex, align-items center, justify-content center, gap 8px. Contains icon "ti ti-map-pin" and text "get directions".

A horizontal rule: border none, border-top var(--border-hairline).

Detail rows, each with display flex, justify-content space-between, align-items center, padding 4px 0:
Address — 154 Ludlow St
Neighborhood — Lower East Side
Type — Café
Price range — $

Label styles: font-size var(--text-sm), color var(--color-ink-muted). Value styles: font-size var(--text-sm), color var(--color-ink), font-weight var(--weight-medium).

Map thumbnail: a div with class "map-thumb", background #D8D4CC, border-radius var(--radius-lg), height 120px, position relative, overflow hidden, cursor pointer. Add simple horizontal and vertical lines to suggest a road grid. Add a pin SVG at center using the WF-Here pin path in red. Add an "open map" overlay badge: position absolute, bottom 8px, right 8px, background rgba 0 0 0 0.5, color white, font-size var(--text-xs), padding 3px 8px, border-radius var(--radius-sm).

Submitted by line: a p tag centered, font-size var(--text-xs), color var(--color-ink-faint). Text: "submitted by the community · verified by wf—here".

RESPONSIVE — mobile below 768px

Photo hero: left image only, full width, height 200px. Right grid hidden. Main content: single column, sidebar below main. Sidebar: not sticky. Save and directions become a fixed bottom action bar: position fixed, bottom var(--bottom-nav-height), left 0, right 0, background var(--color-surface), border-top var(--border-hairline), padding 8px 12px, display flex, gap 8px, z-index 50.

WHAT TO DELIVER

Complete place detail page. Bathroom reveal interaction working. Copy button working. Sticky sidebar on desktop. Fixed action bar on mobile. Removal request link visible at bottom. No console errors.

----------------------------------------------------------------

QA — open /place/index.html. You should see the photo hero at top, then the two-column content layout. Click "reveal code" and confirm the bathroom code 4281 appears and the button text changes to "hide". Click it again and confirm it hides. Click the copy button on the code and confirm it shows "copied" briefly. On mobile the sidebar should disappear and the save and directions buttons should appear fixed above the bottom nav.
