CONTEXT LINE — paste this first, then paste the brief below it.

We are building WF-Here, a map-based workspace finder. The design system is set up in /css/tokens.css. Do not change any existing files unless I explicitly ask. Always reference tokens.css values rather than hardcoding colors, fonts, or spacing.

----------------------------------------------------------------

BRIEF 12 — WIFI PASSWORD GATE

Reveal the real wifi password to logged-in users on the place detail page. Guests see a locked state with a sign in nudge. This brief is short and surgical — only /place/index.html and /js/db.js are touched.

BACKGROUND

In Brief 10 the place detail page fetches the full listing from Firestore including the wifiPassword field. The password is stored in a JavaScript variable but never rendered in the HTML — everyone sees the locked state. In Brief 11 auth was wired up. Now we connect the two.

SECURITY NOTE FOR CURSOR

Do not expose the wifi password in the HTML source at any point. The password must only be inserted into the DOM after confirming the user is logged in via onAuthStateChanged. Never store it in a data attribute, hidden input, or any HTML element that exists before auth is confirmed.

CHANGES TO PLACE DETAIL PAGE

In /place/index.html find the wifi section. It currently shows the locked state for everyone. Update the JavaScript logic as follows.

After the listing loads from Firestore store the wifiPassword value in a local JavaScript variable called wifiPassword. Do not put it in the DOM yet.

Call onAuthChange from auth.js. Inside the callback:

If the user is logged in and wifiPassword is not empty: replace the locked state with the revealed state. The revealed state shows the password in a div with class "wifi-pw-value" using font-family var(--font-mono), font-size var(--text-sm), color var(--color-ink), background var(--color-surface), border var(--border-hairline), border-radius var(--radius-md), padding 5px 10px, flex 1. Add a copy button next to it with id "copyWifiBtn" — same copy button style used on the bathroom code. Clicking it copies the password to clipboard and temporarily shows "copied" for 1500ms. Hide the login nudge bar entirely.

If the user is logged in and wifiPassword is empty: show "No password required — open network" in the password row. Font-size var(--text-sm), color var(--color-ink-muted), font-style italic. Hide the login nudge bar.

If the user is not logged in: keep the locked state exactly as built in Brief 10. The login nudge bar remains visible. Clicking the "sign in" button in the nudge bar stores the current page URL in sessionStorage under key "redirectAfterLogin" then navigates to /login/index.html.

CHANGES TO DB.JS

No changes needed. The getListing function already returns the full listing object including wifiPassword. The security comes from only rendering it after auth confirmation, not from hiding it in the database query.

FUTURE SECURITY NOTE

Add a comment in db.js explaining that in a production environment wifi passwords should be stored in a separate Firestore collection with security rules that only allow reads by authenticated users. For now test mode rules cover this. This will be addressed in the security rules brief before launch.

WHAT TO DELIVER

Place detail page showing wifi password to logged-in users and locked state to guests. Copy button working on the revealed password. Login nudge redirecting to login and back correctly. No password ever appearing in HTML source before auth confirmation. No console errors.

----------------------------------------------------------------

QA — open /place/index.html?slug=ludlow-coffee-supply while logged out. Confirm the wifi password row shows the lock icon and "sign in to reveal" text. Click the sign in link in the nudge bar. After signing in confirm you are redirected back to the same place detail page. Confirm the wifi password "ludlow2024" is now visible in the password row. Click the copy button and confirm it briefly shows "copied". Right-click the page and view source — confirm the password string "ludlow2024" does not appear anywhere in the raw HTML source.
